package com.cleanup.todoc.datasource.database;

import android.content.Context;

import com.cleanup.todoc.datasource.Entity.ProjectEntity;
import com.cleanup.todoc.datasource.Entity.TaskEntity;
import com.cleanup.todoc.presentation.model.Project;
//import com.fasterxml.jackson.core.type.TypeReference;
//import com.fasterxml.jackson.databind.ObjectMapper;

import java.io.File;
import java.io.IOException;
import java.lang.reflect.Type;
import java.util.List;
import java.util.Objects;

public class ReaderJson{
    //private ObjectMapper objectMapper;
//
    //public List<TaskEntity> getTaskListByJsonFile(File file){
    //    List<TaskEntity> tasks;
    //    try {
    //        tasks = objectMapper.readValue(file, new TypeReference<List<TaskEntity>>() {});
    //    } catch (IOException e) {
    //        throw new RuntimeException(e);
    //    }
//
    //    return tasks;
    //}
//
    //public List<ProjectEntity> getProjectListByJsonFile(File file){
    //    List<ProjectEntity> projects;
    //    try {
    //        projects = objectMapper.readValue(file, new TypeReference<List<ProjectEntity>>() {});
    //    } catch (IOException e) {
    //        throw new RuntimeException(e);
    //    }
//
    //    return projects;
    //}

}
